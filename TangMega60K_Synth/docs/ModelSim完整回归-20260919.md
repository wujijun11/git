# 当前工程ModelSim完整仿真记录

2026-09-19，ModelSim Intel FPGA Starter Edition 2020.1。测试使用当前源码的独立副本，未修改硬件RTL；结束后SHA256核对通过。

## 结果

- 六组控制/I2S回归通过：分配器、I2S、诊断音源、V2控制、V2 I2S、V2兼容。
- DSP单元通过：增益、负数、64路同相满幅、饱和、ADSR、延迟脉冲响应、旁路与复位。
- 4声部音源通过：81188个样本，测量计算区间最大45拍。
- 64声部音源通过：81366个样本，测量计算区间最大585拍，done累计66次（包含额外尾音用例）。585拍不是整机端到端延迟。
- 64声部、延迟开启的24位标准I2S通过：10862帧、10859非零帧、稳态新增欠载0；启动空FIFO欠载1次。
- 独立PCM分析通过：谐波音色二次/基波约0.499995、三次/基波约0.249996；颤音约5.00381Hz，零交叉估计范围-101.11到101.14音分，在测试容差内。
- 最终各项编译与仿真0错误、0警告。

## 唯一源码改动

sim/tb_control_v2.sv的表情测试实例原来省略10个未使用的输出端口，ModelSim报告11条端口连接警告。已增加显式空连接，未改变刺激、断言或硬件逻辑。针对性复测通过，0错误、0警告。原始警告日志与修正后日志分别保留。

## 证据

结果根目录：sim/audio_results/current_20260919_163111/

- sim/test_*results.log：控制回归；V2最终结果以sim/test_v2_corrected.log为准。
- sim/audio_results/modelsim.log：完整音频日志。
- sim/audio_results/spectrum_validation.json：PCM独立分析。
- sim/audio_results/current_i2s.wlf：本次64声部I2S完整波形。
- verify_results.ps1：检查通过标志、诊断信息及RTL哈希。
- source_hashes.json：RTL校验记录。

在ModelSim Transcript执行以下命令加载已保存波形，无需重跑：

```tcl
do {C:/Users/asus/Desktop/Git/TangMega60K_Synth/sim/audio_results/current_20260919_163111/view_verified.do}
```

默认显示2ms附近稳态：active_count应为64。test_passed仅在整个测试结束时变成1，稳态窗口显示0正常。

本记录为RTL仿真，不替代实际板卡时序、DAC输出音质、64谱峰及传感器到音频输出延迟的硬件验收。
