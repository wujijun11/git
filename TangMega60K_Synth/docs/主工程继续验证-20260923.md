# 主工程继续验证（2026-09-23）

按用户要求暂停RGB颜色和按键位置排查，继续现有64声部主音源。外置DAC尚未到货，本记录只覆盖数字仿真，不表示实物音频通过。

## 本次已完成

- `scripts/test-board-audio.ps1`：`BOARD AUDIO INTEGRATION PASSED frames=2425`，退出码0。覆盖真实音源的标准I2S逐样本比对、64声部启动、停止回收及复位。
- `scripts/test-iverilog.ps1`：六组回归全部通过，退出码0；包括4/64槽位分配、两个分频比的I2S、测试音源、V2表情和兼容接口。测试音源12000帧，欠载0。
- ModelSim Live64事件源：65种场景通过，包括64种中途停止位置、反压及复位。
- ModelSim音源异常场景：32次保持音重触发、32次快速尾音、26组表情包及命令停顿/复位检查通过。已结束的ModelSim测试报告0错误、0警告。
- 更新README和团队协作说明中的过时状态；未修改音源RTL、ROM、引脚或板上配置。

## 仍在执行

完整Live64频谱测试正在采集262144个样本，之后脚本才运行FFT及分析器反例测试。目前不能记为通过。

本次独立仿真目录：`C:/Users/asus/AppData/Local/Temp/codex_live64_a4838676673a491c94f129b19127ffb6`。完成后脚本将结果复制到`sim/live64_results`。检查完整日志中的`LIVE64 SPECTRUM CAPTURE PASSED`及终端最终`LIVE64 ALL PASSED`，不能用旧文件代替本次结果。

复现命令（本机路径）：

```powershell
./scripts/test-board-audio.ps1
./scripts/test-iverilog.ps1
./scripts/test-live64.ps1 -ModelSimBin D:/msim/modelsim_ase/win32aloem -PythonExe C:/Users/asus/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe -SkipBaseline
```

`-SkipBaseline`仅跳过该脚本内的基线回归；上面独立运行了六组控制/I2S及板级集成测试。本次未重跑完整`test-audio.ps1`，不要将历史完整音频结果标记为本次复测。

## 后续主线

1. 收齐本次频谱结果，确认64个独立音高、左右声道、连续供数及释放回收。
2. 确定最终音频时钟：当前板级50MHz/1024=48,828.125Hz，原音高ROM与包络/颤音参数按48kHz配置；需要统一实际采样率与这些参数，不能只修改时钟标注。
3. 根据实际DAC模块和Dock版本核对I2S物理引脚、电平和供电，再增加外部输出约束。
4. 接入队员B的实际演奏事件和独立表情输入，验证并发、反压与松键不丢失。
5. DAC到货后验证模拟输出、音高、噪声及传感器到音频的端到端延迟。

当前板上最后确认下载的是`BoardBringup.fs`按键识别程序；本轮没有重新下载主音源。下载完成也不能替代实物功能验收。

## 主音源重新上板

2026-09-23：已将板上按键识别程序替换为`BoardAudioDiagnostic.fs`，采用SRAM Program。GOWIN Programmer确认目标GW5AT-60B（ID 0x0001481B），下载Finished，用时11.77秒，User Code=0x0000EC03，Status Code=0x70026020。

码流SHA256为D3E2419FB63DF7ED2DE783E72714211B5C9CC12A68A31BAFCCAE4066591914F2，与之前已构建的主音源板级诊断一致。未写Flash。此记录更新前文“板上最后为BoardBringup”的状态。

下载后的声部数、欠载计数和串行样本尚未通过实物读回验收。当前码流没有内嵌GAO抓取配置；后续需增加调试采集入口或外部测量，不能把下载成功当作满负载验证通过。

## 后续GAO实物验证完成

上述读回待办现已推进：新增并下载独立 `BoardAudioGAO.gprj` 调试程序，两次实物采集均为64声部、错误/故障0、启动欠载1，自动启停次数29→120；内部24位标准I2S检查通过。当前SRAM运行此GAO自动测试程序。详细证据和限制见[GAO板内验证](hardware/GAO板内验证-20260923.md)。这不代表外置DAC或模拟音频验收完成。
