# 队员 B 交互工程合并记录（2026-09-24）

来源：`C:/Users/asus/Downloads/Captain_Delivery_20260924_final/TangMega60K_Synth/`，交付说明为同包的 `交付说明.md`。目标基线是已合入队员 A 八成分钢琴与 48 kHz 外置 I2S 预备工程的 `TangMega60K_Synth`。

## 接入范围

- 引入 `event_fifo.v` / `interaction_top.v` 及对应 V1 测试，供原接口工程使用。
- 引入 `event_fifo_v2.v` / `interaction_top_v2.v`，支持 6 位来源编号、按键与和弦的按下快照、松键回放、FIFO 反压和表情快照。
- 引入 `instrument_system_v2.v`，将 B 的输入边界连到现有 `audio_system_v2`，输出 24 位 Philips I2S。参数 `FM_ENABLE` 默认关闭，开启后允许音色 3 的 FM 实验路径；音色 2 沿用 A 的八成分钢琴。
- 对同一来源或和弦槽在未松开时重复收到的按下事件，握手后不再发出第二次音符事件，也不覆盖第一次按下的快照；松开仍释放原音符，避免键盘抖动造成卡音。V2 交互回归增加了这两种场景。
- 更新三个 Gowin 工程文件中的对应源文件列表，加入 B 的 V1/V2/统一顶层仿真入口和综合脚本。`AudioEngine_V2.gprj` 可选择 `instrument_system_v2` 作为统一逻辑顶层。
- 保留 B 的四份交付文档作设计记录，并新增 `scripts/test-interaction.ps1`，用当前主工程 RTL 复测 V1、V2 及统一顶层。

B 交付包中同名的 `i2s_tx.v`、`audio_system_v2.v`、`voice_engine.v`、`adsr.v`、`wavetable_rom.v` 和旧测试台没有覆盖主工程：当前主工程已有启动欠载修复、A 的八成分钢琴和可选 FM 音色。合并前后这几份音频 RTL 及 `tb_i2s_tx.sv` 的 SHA256 一致。

## 验证

- Icarus：`scripts/test-interaction.ps1` 五组通过（V1、V2、统一顶层音色 0/2/3），统一顶层每组 0 次 I2S 欠载，并检查音符/音色命令、表情参数、事件 FIFO 守恒、I2S 分频和非零串行数据。
- 既有六组控制/I2S Icarus 回归通过；48 kHz 外置 I2S 板级仿真通过启停、重启和复位，未更改原板级入口。
- ModelSim 完整九组回归通过，入口为 `scripts/test.ps1 -ModelSimBin 'D:/msim/modelsim_ase/win32aloem'`；其中统一顶层分别用音色 0/2/3 完成事件到 I2S 的仿真，三组欠载均为 0。交互回归包含重复按下后按原快照松开的检查。
- Gowin V1.9.12.03 在独立目录综合 `instrument_system_v2` 成功：Logic 6387/59904，Register 5817/60780，BSRAM 54/118。综合报告位于本机 `C:/Users/asus/Desktop/Git/tmp/merge_b_20260924/synthesis/impl/gwsynthesis/AudioEngine_V2_syn.rpt.html`。该综合是逻辑顶层验证，尚无物理引脚、PLL、CST、SDC、布局布线或新位流。

统一顶层采用现有音频时钟接口，预期输入 49.152 MHz；48 kHz 板级预备工程仍使用独立自检事件源。实物键盘扫描、消抖、传感器驱动与引脚连接尚未实现，因此 B 的输入还不能在板上直接演奏，也未烧录 B 合并后的设计。
