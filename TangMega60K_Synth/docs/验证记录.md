# 队长工程验证记录

## 2026-09-14 ModelSim 界面复核

在 ModelSim Intel FPGA Starter Edition 2020.1 中，将已加载的 `tb_i2s_tx` 从 110 us 继续运行至约 1252.19457 us。Transcript 显示：

```text
I2S CASE PASSED DIV=1 frames=62 nonzero=47 stalls=5034 full_exchange=40
I2S CASE PASSED DIV=8 frames=60 nonzero=47 stalls=38840 full_exchange=40
ALL I2S TESTS PASSED
```

两组完成标志均为 1，仿真正常停在测试台的 `$finish`。欠载和复位场景属于主动注入的测试。
下文引用的日志及综合报告是本机生成文件，不随仓库上传；可运行验证脚本重新生成。

## 第二阶段：I2S输出与控制器集成

验证日期：2026-09-13。

| 检查 | 结果 | 证据 |
|---|---|---|
| GOWIN V1.9.12 综合 | 通过，顶层 captain_system_top | impl/gwsynthesis/TangMega60K_Synth.log |
| 目标器件 | GW5AT-60B，GW5AT-LV60PG484AC1/I0 | TangMega60K_Synth.gprj |
| 综合资源（总计） | 858 LUT、50 ALU、843寄存器 | impl/gwsynthesis/TangMega60K_Synth_syn_rsc.xml |
| 新增音频模块资源 | 184 LUT、36 ALU、195寄存器 | 同上，u_audio |
| ModelSim 2020.1 控制器回归 | 4与64声部均通过，0 errors / 0 warnings | sim/test_results.log |
| ModelSim 2020.1 I2S测试 | 两种分频均通过，0 errors / 0 warnings；ALL I2S TESTS PASSED | sim/test_i2s_results.log |
| 默认分频8 | 完成任务时核对60帧，47帧非零；40次FIFO满时同时出入队 | 同上 |
| 边界分频1 | 完成任务时核对62帧，47帧非零；40次FIFO满时同时出入队 | 同上 |
| Icarus独立仿真 | 同一I2S测试台通过，结果与ModelSim一致 | sim/tb_i2s_tx.sv |

接收器只通过外部BCLK、WS、DATA恢复左右24位样本，与输入握手队列逐帧比较，
同时核对I2S一位延迟、32位时隙、时钟周期、补零、FIFO容量与欠载统计。
持续供数时并行测试了命令反压、note-on、release和done回收。

综合资源是控制器与音频输出接口的资源，**不包含64振荡器、ADSR、混音和效果器**。
尚未验证：板级时钟与复位、CST/SDC、布局布线与时序收敛、DAC发声、音质、端到端演奏延迟。
无当前板卡可直接下载的完整发声比特流。

## 第一阶段历史记录

以下为第一阶段结果。当前impl目录的报告已由第二阶段综合结果覆盖；
其中u_control子模块仍保留相同资源，第一阶段RTL没有改动。

验证日期：2026-09-13（综合于2026-09-12完成，RTL未变更）。

| 检查 | 结果 | 证据 |
|---|---|---|
| GOWIN V1.9.12 综合 | 通过 | impl/gwsynthesis/TangMega60K_Synth.log |
| 目标器件 | GW5AT-60B，GW5AT-LV60PG484AC1/I0 | TangMega60K_Synth.gprj |
| 综合资源 | 674 LUT、14 ALU、648寄存器 | impl/gwsynthesis/TangMega60K_Synth_syn_rsc.xml |
| ModelSim 2020.1 编译和仿真 | 0 errors、0 warnings；ALL TESTS PASSED | sim/test_results.log |
| 4声部与64声部行为测试 | 两组均通过 | sim/tb_voice_allocator.sv |
| Icarus独立行为仿真 | 两组均通过（2026-09-12） | 与ModelSim相同测试台 |

以上资源仅为复音管理控制部分，不能作为完整64复音乐器的资源占用。
尚未验证：布局布线、实际最高工作频率、板卡IO和供电、I2S、DAC发声、音质、端到端延迟。
没有生成或下载可用于本板的完整发声比特流。

测试包含输入事件、输出反压、同音重触发、尾音回收、满载拒绝、异常完成、复位、并发事件优先级。
声音引擎和传感器暂由测试台模拟接口行为。
